## 1. Localización de la API de Clima

- [x] 1.1 Modificar `src/app/services/weather.service.ts` para agregar el parámetro `&lang=es` en la llamada GET a OpenWeatherMap

## 2. Localización de Metadatos HTML

- [x] 2.1 Modificar `src/index.html` para cambiar el atributo de idioma a `lang="es"` y la etiqueta de título a `<title>ClimaApp</title>`
