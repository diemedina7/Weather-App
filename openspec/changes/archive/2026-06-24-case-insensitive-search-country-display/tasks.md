## 1. Implementación de Búsqueda Insensitiva

- [x] 1.1 Modificar `src/app/services/cities.service.ts` para normalizar ambas cadenas a minúsculas usando `.toLowerCase()` durante la comparación `.includes()`

## 2. Inclusión de País en el Desplegable

- [x] 2.1 Modificar `src/app/components/select/select.html` para interpolar el país (`{{ city.country }}`) junto al nombre de la ciudad (`{{ city.name }}`) en cada ítem de la lista
